Issue: #76 - [0.2.0 draft] Core: plan-then-execute handshake before agent dispatch
State: open
Labels: enhancement, agentic, draft
URL: https://github.com/conduit-harness/conduit/issues/76
Workspace: C:\Users\Tom\repositories\conduit\.conduit\workspaces\76\attempt-8
You are picking up an open GitHub issue from the conduit-harness/conduit repository — Conduit's own codebase. This is dogfooding: Conduit dispatched you, and you are now working on Conduit itself.

**Issue:** #76 — [0.2.0 draft] Core: plan-then-execute handshake before agent dispatch
**URL:** https://github.com/conduit-harness/conduit/issues/76
**Labels:** enhancement, agentic, draft
**Workspace:** C:\Users\Tom\repositories\conduit\.conduit\workspaces\76\attempt-8 (branched from `main`)
**Attempt:** 8

**Description:**
**Status: draft / placeholder.** Do not pick this up — it needs design before any implementation.

## Intent (from 0.1.0 launch post)

Agents should be able to propose a plan for review before execution, reducing surprises in autonomous runs.

## Open questions

- **Where does the plan live?** Tracker comment, separate state file, both?
- **Trigger:** opt-in workflow flag, per-issue label (e.g. `plan-first`), or both?
- **Approval mechanism:** emoji reaction on the comment, label transition, slash-command in a comment, web hook?
- **Re-plan loop:** what happens if a human rejects? New comment thread? New attempt counter?
- **Spec alignment:** does the Symphony spec already define a planning lifecycle event, or do we extend it?
- **Runner support:** does each runner (Claude CLI / Codex CLI / Aider) have a plan-only mode, or do we drive it via prompt construction?

## Done when

A scoped issue (or set of scoped issues) replaces this placeholder, with concrete acceptance criteria and a chosen design path.

## Working rules

- You are inside an isolated git worktree. **Commit early, commit often** — make focused commits on the current branch as you complete each logical step.
- Read the relevant code before changing it. Cite file paths with line numbers in your final summary.
- Run `pnpm typecheck` and `pnpm test` before declaring success. If a test is missing for the change, add it.
- Follow the conventions in `CONTRIBUTING.md` and `docs/UBIQUITOUS_LANGUAGE.md`.
- Keep the change minimal. Do not refactor adjacent code or modify unrelated files.
- If the task is ambiguous or blocked on a decision the issue does not answer, stop and report; do not guess.

## On finish

1. Stage and commit all changes with a descriptive (conventional-commits) message.
2. Push the branch: `git push -u origin HEAD`.
3. Open a PR: `gh pr create --title "Issue: #76 — [0.2.0 draft] Core: plan-then-execute handshake before agent dispatch" --body "Closes https://github.com/conduit-harness/conduit/issues/76"`.
4. Report file diffstat and test output.

If you halted without completing the task, end with `STATUS: halted` on its own line and explain what blocked you.